pipeline {
    agent { label 'aws-agent' }
    stages {
        stage('Run Build') {
            steps {
                sh 'cd run-on-agent-complex'
                sh 'ls -a'
                sh 'sudo chmod +x build.sh '
                sh './build.sh' 
            }
        }
    }
}